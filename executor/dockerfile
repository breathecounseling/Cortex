FROM python:3.11-slim

WORKDIR /app

COPY executor/requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY executor/ ./executor/

CMD ["python", "-m", "executor.middleware.router"]
