version: "3.9"

services:
  cortex-executor:
    build: .
    container_name: cortex-executor
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount repo into container for live code changes (optional)
      - .:/app
    restart: unless-stopped